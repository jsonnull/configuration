snippet vitest "vitest file"
  import { describe, expect, test } from 'vitest';

  describe('${1}', () => {
    test('${2}', () => {
      ${0}
    });
  });

snippet useFakeTimers "useFakeTimers"
	import { afterEach, beforeEach, vi } from 'vitest';

	beforeEach(() => {
		vi.useFakeTimers();
	});

	afterEach(() => {
		vi.useRealTimers();
	});
